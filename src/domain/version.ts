/**
 * Single source of truth for the app version displayed to users.
 * Keep in sync with android/app/build.gradle (versionName + versionCode).
 */
export const APP_VERSION = "v5.2.0-stable";
export const APP_VERSION_CODE = 50200;
export const APP_BUILD_DATE = "2026-04-17";
