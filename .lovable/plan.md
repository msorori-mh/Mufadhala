

## ملخص التحسينات المطلوبة

تحسين شامل لعرض الصفحة الرئيسية على شاشات الجوال: تصغير البطاقات الثلاث (شمولية المنهج، تعزيز الفهم، محاكاة بيئة الاختبار)، وضع الأيقونة في بداية سطر العنوان بدلاً من أعلى البطاقة، وتقليل المساحات الفارغة في جميع أقسام الصفحة.

## التغييرات في `src/pages/Index.tsx`

### 1. بطاقات الميزات الثلاث (Academic Core Pillars)
- **تحويل التخطيط على الموبايل** من عمودي (أيقونة فوق النص) إلى **أفقي** (أيقونة بجانب العنوان في نفس السطر)
- تصغير padding من `p-4` إلى `p-3`
- تصغير الأيقونة من `w-10 h-10` إلى `w-8 h-8`
- تقليل gap بين البطاقات من `gap-4` إلى `gap-2.5`
- تقليل padding القسم من `py-8` إلى `py-5`

### 2. تقليل المساحات الفارغة في باقي الأقسام
- **Hero Hook**: تقليل `py-5` إلى `py-3`
- **AI Assistant**: تقليل `py-4` إلى `py-2.5`
- **Value Badges**: تقليل `py-6` إلى `py-4` وتقليل gap من `gap-3` إلى `gap-2`
- **Stats Bar**: تقليل `py-6` إلى `py-4`
- **Trust Note**: تقليل `py-6` إلى `py-4`
- **Social Proof**: تقليل `py-8` إلى `py-5`
- **FAQ**: تقليل `py-8` إلى `py-5`

### 3. التفاصيل التقنية
- استخدام responsive classes مثل `py-3 md:py-5` للحفاظ على تجربة سطح المكتب
- البطاقات على الموبايل: `flex items-start gap-3 text-right` بدلاً من `text-center`
- على الشاشات الكبيرة (`md:`): تبقى البطاقات بالتخطيط العمودي الأصلي

