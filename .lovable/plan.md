
## Goal
1. Add a header at the top of `/install` with logo + "مُفَاضَلَة | Mufadhala".
2. Compact the entire page — tighter spacing, smaller paddings, denser cards, better use of empty space.

## Exploration
Quick check needed: existing logo asset in `/public/` and how Welcome/Index pages render the brand name.
