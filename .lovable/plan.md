

## إخفاء خيار اللغة من صفحة الإعدادات

التطبيق يعمل بالعربية فقط حالياً، لذا سيتم إزالة قسم تبديل اللغة من بطاقة "المظهر واللغة" في صفحة الإعدادات.

### التعديل

**ملف واحد**: `src/pages/Settings.tsx`

1. حذف state الخاص باللغة (`language`, `setLanguage`) و useEffect المرتبط به
2. حذف عنصر `Select` الخاص باللغة و `Separator` الذي يسبقه من بطاقة "المظهر واللغة"
3. تحديث عنوان البطاقة من "المظهر واللغة" إلى "المظهر" وتغيير الأيقونة من `Globe` إلى أيقونة مناسبة (مثل `Moon`/`Sun`)
4. حذف استيراد `Globe` و `Select` components إذا لم تعد مستخدمة

