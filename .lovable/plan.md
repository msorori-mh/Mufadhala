

## الخطة: فقاعة "تم تعديل مدة الاختبار"

### الملف
`src/pages/past-exam/ModeSelector.tsx`

### التغيير
داخل دالة `handleDurationConfirm` (السطر 109-120)، إضافة استدعاء `toast.success()` من sonner بعد حفظ المدة الجديدة بنجاح، يعرض رسالة:
- **العنوان**: "تم تعديل مدة الاختبار"
- **الوصف**: `"المدة الجديدة: ${customDuration} دقيقة"`

### الاستيراد المطلوب
إضافة `import { toast } from "sonner"` في أعلى الملف.

### ملاحظات
- الفقاعة تظهر فقط عند تأكيد التعديل من نافذة اختيار المدة (لا تظهر عند فتح النافذة فقط).
- لا تغييرات على المنطق الحالي للحفظ في localStorage أو فتح نافذة التأكيد.

